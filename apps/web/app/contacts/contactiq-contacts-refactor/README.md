# ContactIQ Contacts refactor

Kopiraj mapo `apps/web/app/contacts` v svoj projekt in prepiši obstoječo mapo `contacts`.

Ta prvi varni refactor:
- ohrani trenutno funkcionalnost,
- premakne tipe v `types.ts`,
- premakne konstante v `constants.ts`,
- premakne helper funkcije v `utils.ts`,
- premakne enrichment drawer v samostojno komponento.

Po kopiranju zaženi frontend in preveri TypeScript/build.
